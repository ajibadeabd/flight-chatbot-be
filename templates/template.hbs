<head>
    <title>{{ title }}</title>
    <link rel="stylesheet" type="text/css" href="{{ css_path }}">
</head>
<body>
    <header>
        <h1>{{ header }}</h1>
    </header>
    <main>
        <p>{{ content }}</p>
    </main>
    <footer>
        <p>&copy; {{ current_year }} Your Company</p>
    </footer>
</body> 